/** @jsx React.DOM */
define(function (require) {
    'use strict';

    var React = require("react");

    return React.createClass({

        render: function(){
            return(
                <div>
                    <h3>There are no known issues. Hurrah!</h3>
                </div>
            );

        }

    })
});